# Phase B Story Backlog (2026-09-20)

This splits master §16's **Phase B — context + agents + phone** into 45 stories. Order = dependency order (top to bottom; every dependency has a lower number than its dependent — **one exception**: US-B19 depends on US-B20, because `taskLoop` imports `routeByRule`; rather than renumber, this is recorded only in the dependency column. 2026-09-20 cross review M14). This backlog is the input to 5 plan documents (input to the `writing-plans` skill), and each plan document lands in the file its `Plan` column points at.

- Parent: `docs/spec/00-omnis-design.md` v1.0 §10·§11·§12·§14·§16·§19, appendix A1~A7 v1.0.
- Contract: `2026-09-20-phase-a-interfaces.md` (Phase A) + `2026-09-20-phase-b-interfaces-delta.md` (what this Phase adds). Identifiers are **copied verbatim** from the delta document.
- Baseline code: `main` 131a042 (Phase A 35 stories + Wave 4 complete, Wave 5 in progress — manual archive US-A36, kinso polish US-A37, `tools/auth-kit`).
- Design: `docs/design/DESIGN-DIRECTION.md` (kinso, light theme by default).

---

## 0. Decisions this backlog locks in (B-D1~B-D8)

| # | Decision | Rationale | If reversed |
|---|---|---|---|
| **B-D1** | **Do not use mem0-ts.** Vector memory is a thin pgvector layer that `@omnis/memory` attaches directly to `public.memories`. mem0's fact-extraction prompt structure (ADD/UPDATE/DELETE verdicts) is used for **reference only**; no code or strings are copied | Measured (2026-09-20, after `npm pack mem0ai@3.2.0` and inspecting `dist/oss/index.d.ts`): ① `type VectorStore` is exported but is **types only**, and `MemoryConfig.vectorStore` is `{provider: string, config: VectorStoreConfig}` plus the static factory `VectorStoreFactory.create(provider, config)`, so **there is no slot for plugging in an instance of an external implementation** → the "custom VectorStore adapter" premise of A3-D12 breaks (S-A3-1 = **FAIL**). ② The bundled `PGVector` **creates its own tables** via `createDatabase`/`createCol` — there is no way to express `memories`' typed columns (kind/scope/source_kind/4-timestamp/superseded_by) or the partial HNSW (`WHERE invalidated_at IS NULL`). ③ graph memory was removed in v2.0.0 (`26` VERIFIED) — entity/relation are our own tables anyway. ④ The package drags in `@langchain/core` on the type path and bundles 20 kinds of vector store drivers | A3-D12's fallback path (= this decision) is already canonical, so there is nothing to reverse. Revisit if mem0 opens an instance-injection API |
| **B-D2** | Configuration is a **single `settings` key-value table** (`key text PK, value jsonb`). Do not split the allowlist, autonomy rules, cost cap, quiet hours, and retention thresholds into typed tables | A3 has no settings table (exhaustive check). Every value the Settings screen uses is a single row for a single user, so there is no reason to create 6 tables. `ponytail: kv + zod parser; promote to columns when a lookup-by-value pattern appears` | If multi-user (v2) arrives, either add `user_id` or decompose into typed tables |
| **B-D3** | **Avatars are initials only.** Do not create an `avatar_url` column on `persons` | `RowAvatar` in `packages/ui` already supports `{kind:'initials'}`. Remote avatars mean channel API calls + an image cache + privacy (outbound requests), all new costs, and the information density of a kinso row is satisfied by initials | If real photos become necessary, add the `avatar_url` column together with a local cache directory (`RowAvatar` already has a `photo` branch) |
| **B-D4** | The iPhone is the **installed PWA from `apps/web`**, served by the hub on the same origin via Tailscale Serve. No separate hosting, no separate domain, no Tauri iOS | Master §19 Q1 default; the two `tailscale serve` lines in A6 §3 are already exactly that shape | Tauri iOS is Phase D |
| **B-D5** | Every Phase B story **must be verifiable with seed data/fixtures alone** (Logan's decision: real account connection happens once, later). Adapters, ingestion, and notifications are all accepted via fixture replay or a mock server | Logan's 2026-09-20 decision. Phase A's adapter contract tests (A1 §1.7) are already the same shape | If real connection is moved up, run the spikes first (A1 §4 `A1-⑥`/`A1-⑦`) |
| **B-D6** | **Delegation is automatic proposal + approved execution.** Full autonomy applies only when the per-runtime and per-repo allow rules in `settings` are enabled, and every default is off | Master §11·§19 Q10, A4 §4.4 | — (confirmed by Logan) |
| **B-D7** | **Hermes is a read-only session** (only `origin:'human'`). Excluded from delegation targets | Master §19 Q7, A2 §4.4 | Phase C, once S-A2-5 passes |
| **B-D8** | Cost cap **$60**, VIP·sensitive reserve 10%. Even in `degraded`, the sensitivity rule (forced T2) is never broken | Master §14·§19 Q4·Q11, A4 §12.4 | The cap and reserve ratio are editable in Settings (recorded in the audit log) |

---

## 1. Scope differences from master §16 (explicitly recorded)

The Phase B row of master §16 reads "3-layer memory + ingestion, context drafts + notifications, morning briefing, auto-archive + nightly digest, unified search (⌘K), PWA iPhone + Web Push, Outlook·Telegram, Hermes read-only session". This backlog **pulls 4 items from the Phase C row forward** into it:

| Item pulled forward | Original phase in master §16 | Story in this backlog | Why |
|---|---|---|---|
| Todo extraction·reminders + delegation (auto-propose + approve) | C | US-B19, US-B20, US-B29 | The morning briefing's `commitments` section (A4 §6.2) reads `tasks` — as long as the briefing is in Phase B, todos must be in B too or the section stays empty |
| Network + follow-up | C | US-B22, US-B30 | The draft loop reads the "as of now" view of `persons`/`entities` (A4 §3.2). Since identity resolution (US-B03) is in B, the people screen comes along with it |
| Note routing | C | US-B21, US-B31 | The routing loop is just one `search_memory` call + `propose_route`, making it the cheapest consumer on top of the memory layer |
| Person label (person_label) | C | Absorbed into US-B03 | `classify` already outputs `person_label` (Phase A US-A23) — it just had no consumer |

**What stays in Phase C**: the 3 capture channels (KakaoTalk/LinkedIn/WhatsApp), promoting claude-ds·Hermes to delegation targets, and read-only import of terminal sessions. The master §16 table needs updating to reflect this difference (§6 open question 1).

---

## 2. Story backlog (45 stories)

The `Tier` value follows A7 §3's model assignment rules (`DeepSeek` forces a Sonnet+ review). The `Plan` value is the plan file slug from §3.

**2026-09-20 cross review applied (M14)**: US-B20 (delegation) depends on **`B07`**, not `B19` — `routeByRule()` runs directly on top of the tool palette and does not read `tasks`. Conversely, US-B19 (`taskLoop`) imports `routeByRule`, so **`B20` is added to B19's dependencies**. The agents plan's execution order (Task 17 → 15 → 16) matches this direction.

| ID | Goal | Deliverable | Verification command | Tier | Depends on | Plan |
|---|---|---|---|---|---|---|
| US-B01 | `@omnis/memory` scaffold + thin pgvector layer (B-D1): `embed()` (Ollama `nomic-embed-text-v1.5`, 768d, batched + on failure store `embedding=NULL`), `upsertMemory()`, `searchMemories()` (through the partial HNSW, wrapping the A3 §12 vector functions), `invalidateBySource()` (bulk-set `invalidated_at` by `source_kind`+`source_ref`, never delete). Record the rationale for the mem0-ts FAIL verdict in `result.md` | `packages/memory/src/{index,embed,store,search}.ts`, `tools/spikes/s-a3-1-mem0-vectorstore/result.md` | `pnpm --filter @omnis/memory test:integration` | Sonnet | — | memory-ingestion |
| US-B02 | self-model 3-file (`USER.md`/`VOICE.md`/`PROJECTS.md`) loader + frozen snapshot: initialize the git repo (`~/.omnis/self-model/`), check the per-file token caps (1,200/1,500/1,500, A4 §12.3), `snapshot()` returns the immutable string for `cachedPrefix` plus its `sha256`. On cap overflow, a warning system Item | `packages/memory/src/self-model.ts`, `ops/self-model/README.md` | `pnpm --filter @omnis/memory test` | Sonnet | — | memory-ingestion |
| US-B03 | person identity resolution (A3 §10): 6 deterministic per-channel `handle_norm` transforms, 4-step resolution algorithm (identity lookup → cross-channel email match → create new with `verified=false` → never guess), `mergePersons()`/`splitIdentity()` transactions + `person_merges` + `audit_log`. Wire `ingest.sink` to populate `items.author_person_id` (Phase A left it empty) | `packages/kernel/src/identity.ts`, `packages/kernel/src/ingest.ts`(modified) | `pnpm --filter @omnis/kernel test:integration` | Opus | — | memory-ingestion |
| US-B04 | bi-temporal `entities`/`relations` write API: `upsertEntity()` (on a live-unique conflict, set `invalidated_at` on the existing row + insert a new one), `assertRelation()`, `invalidateEntity()`, `asOf(ts)` query (the 3 conditions `valid_from`/`valid_until`/`invalidated_at`). Write paths that do not populate all 4 timestamps are blocked by the type system | `packages/memory/src/entities.ts` | `pnpm --filter @omnis/memory test:integration` | Opus | B01 | memory-ingestion |
| US-B05 | Context assembler (A4 §1.3) + `<data>` normalization·injection tagging (A4 §1.4·§11.1): `buildContext(req)` → `{cachedPrefix, volatile, tokenEstimate, truncated, provenance}`, cache-boundary discipline (timestamps·nonces go after the boundary), 5-step truncation order (USER.md and the last 3 turns are inviolable), NFKC + zero-width stripping·HTML strip·no base64 decoding·URL shortening·nonce substitution | `packages/agents/src/context/{assemble,normalize}.ts`, `packages/agents/test/injection-set.test.ts`(20 cases) | `pnpm --filter @omnis/agents test` | Opus | B01, B02, B04 | memory-ingestion |
| US-B06 | Loop runtime contract (A4 §1.1·§1.2·§1.6): `LoopSpec`/`LoopResult`/`LoopTrigger`, loop registry + kernel scheduler·event wiring, budget enforcement (`LoopBudgetError`), 7 common failure handlers (one retry on timeout → escalate one tier → `failed` + system Item; schema violation; tool-not-found quarantine 24h; empty `injection_flags` → no artifact produced; 3 failures in 24h → `agent_optout`). Every run is one `recordRun`/`finishRun` pair | `packages/agents/src/loop/{spec,registry,run}.ts` | `pnpm --filter @omnis/agents test` | Opus | B05 | agents |
| US-B07 | tool palette (A4 §1.5): 7 read tools (`read_thread`/`search_memory`/`read_person`/`read_entity`/`read_calendar`/`read_tasks`/`read_session`) + 6 `propose_*` tools (`propose_label`/`propose_draft`/`propose_task`/`propose_delegation`/`propose_route`/`propose_self_model_patch`). `propose_*` only stores. A unit test that fails if any of 12 phantom tools is in the registry + a lint rule banning `packages/agents/**` → `packages/kernel/src/egress/**` imports | `packages/agents/src/tools/*.ts`, `biome.jsonc`(modified) | `pnpm --filter @omnis/agents test && pnpm lint` | Opus | B06 | agents |
| US-B08 | L9 ingestion core (A4 §10.3~§10.5): 3 chunkers (documents 500~800 tokens / overlap 100, code at function·class boundaries, calendar 1 event = 1 chunk), T1 extraction → populate 4 timestamps on `memories`+`entities`+`relations`, source cursor table (`ingest_sources`), failure handling (3 backoffs, skip chunks that fail parsing, 3 consecutive failures → dead-letter system Item), calendar source (`calendar_events` → memories) | `packages/memory/src/ingest/{chunk,extract,source}.ts`, `packages/db/migrations/0010_ingest_sources.sql` | `pnpm --filter @omnis/memory test:integration` | Opus | B01, B04, B06 | memory-ingestion |
| US-B09 | Local ingestion (mini): FSEvents subscription + one rescan at boot, per-host folder allowlist (`settings`, empty by default), A4 §10.2 hard exclusion rules (decided by path, contents never inspected) + `.gitignore` merge + 2MB cap + skip binaries containing NUL bytes | `packages/memory/src/ingest/local-mini.ts`, `packages/memory/src/ingest/deny.ts` | `pnpm --filter @omnis/memory test` | Sonnet | B08 | memory-ingestion |
| US-B10 | Local ingestion (MacBook): implement the `ingest.scan(roots, since)`/`ingest.read(path)` RPCs in `local-agent` (A2 §3.2's 3 caps: allowlist ∩ `allowed_roots` intersection + `realpath` re-check, unconditional rejection of secret files, 1MB truncation) + hub consumer (rides the `drive_poll` tick, catches up via `since` when offline) | `apps/local-agent/src/ingest.ts`, `packages/memory/src/ingest/local-macbook.ts` | `pnpm --filter @omnis/local-agent test` | Opus | B08 | memory-ingestion |
| US-B11 | Drive + GitHub polling ingestion: Drive `changes.getStartPageToken()` baseline → `changes.list(pageToken)` + `includeRemoved=true` tombstones → `invalidated_at`, GitHub ETag `If-None-Match` (on 304 do not fetch the body) + rate-limit header backoff + repo allowlist. On token loss, re-establish the baseline (no full rescan) | `packages/memory/src/ingest/{drive,github}.ts` | `pnpm --filter @omnis/memory test` | Sonnet | B08 | memory-ingestion |
| US-B12 | recall evaluation harness: 50 cases in `eval/memory_recall.jsonl` (each including the expected `source_kind`+`source_ref`+as-of time), `pnpm eval:memory` prints recall@10 (target ≥ 0.80). **Hard gate**: CI fails if even one `memories.source_ref` matches an A4 §10.2 exclusion pattern | `tools/eval/memory-recall.ts`, `eval/memory_recall.jsonl` | `pnpm eval:memory` | Sonnet | B08, B09, B10, B11 | memory-ingestion |
| US-B13 | L2 reply draft loop (A4 §3): trigger (`item.labeled` + `needs_reply_score ≥ 0.5`, 20s debounce), 7 context slots, `register` rule determination, Deliberate 4 steps (plan→gather≤3→draft→6-point self-check), 8 per-channel length·format variants, **60s SLA** (past 55s, emit a `meta.pending` placeholder draft first), 6 tier escalation conditions (reuse the same context once, link via `escalated_from`) | `packages/agents/src/loops/draft.ts`, `packages/agents/src/draft/{register,selfcheck}.ts` | `pnpm --filter @omnis/agents test` | Opus | B05, B07 | agents |
| US-B14 | Cost meter (A4 §12.4): `costState()`/`POLICY` with 5 states, separate reserve accounting (`model_tier='T2' AND (vip OR sensitivity≠normal)`), loop gate (the scheduler·loop entry points read `Policy` and skip·demote), `cost_daily` aggregate view + 00:05 job, state transitions → `audit_log` + system Item. Even in `degraded`, the sensitivity rule is not broken | `packages/kernel/src/cost/governor.ts`, `packages/kernel/src/jobs/cost-daily.ts` | `pnpm --filter @omnis/kernel test:integration` | Opus | B06 | agents |
| US-B15 | 3-tier notification router (A4 §3.6): immediate/batched/silent classification, global quiet hours 23:00~07:00 (the only exception is `vip AND priority='now'`, turned off in Settings), the batch job (`push_batch`, 09/12/15/18 KST) collapses it all into a single "N drafts ready". The body carries **only the first 80 characters** | `packages/kernel/src/notify/{tier,batch}.ts` | `pnpm --filter @omnis/kernel test` | Sonnet | B13 | agents |
| US-B16 | Web Push VAPID keypair generation·storage: Keychain `omnis.webpush.vapid_private`/`…public`, injected as env by `omnis-run-with-secrets.sh`, rotation procedure documented (A6 §9, 6 steps) | `ops/scripts/gen-vapid.sh`, `ops/mini/RUNBOOK.md`(modified) | `bash ops/scripts/gen-vapid.sh --check` | Haiku | — | ops |
| US-B17 | 2 notification delivery paths: macOS local notifications (Tauri notification plugin, click → `omnis://thread/{id}` deep link) + a Web Push sender (VAPID signing, lookup in `push_subscriptions`, 2 actions `Approve`/`Open`, clean up the subscription on a 410/404 response). Send failures are not swallowed silently — they become a system Item, not an `agent_runs` row | `apps/desktop/src-tauri/src/notify.rs`, `packages/kernel/src/notify/webpush.ts` | `pnpm --filter @omnis/kernel test` | Sonnet | B15, B16 | agents |
| US-B18 | L8 auto-archive loop (A4 §9) + 7-day undo: the 5 hard gates first (sensitive·VIP·pending approval·injection_flags·kind), verdicts ①③④ are pure SQL (T0), only ②·④-b are T1 (`confidence ≥ 0.85`, below that nothing is archived), record `items.meta.archived_by`, 22:00 sweep job, `undoArchive()` (`archived`→`received` + `audit_log` + exclude that thread for 30 days). **No hard-delete path** | `packages/agents/src/loops/auto-archive.ts`, `packages/kernel/src/archive.ts` | `pnpm --filter @omnis/agents test && pnpm eval:archive` | Opus | B06 | agents |
| US-B19 | L3 todo extraction + reminders (A4 §4): 3 triggers (`item.labeled`/`item.sent`/cron 09·14·19), precision first (`confidence < 0.70` is not even stored, max 3 per item, if the `duplicate_of` cosine > 0.82 add `source_item_id` to the existing task), mark `due_basis='inferred'`, the reminder job is pure SQL in 3 groups with no LLM | `packages/agents/src/loops/task.ts`, `packages/kernel/src/jobs/task-remind.ts` | `pnpm --filter @omnis/agents test` | Sonnet | B07, B20 | agents |
| US-B20 | Delegation proposal + L4 judgment + approved execution (A4 §4.4·§5, B-D6): `DelegationHints` extraction (regex, not an LLM) + `routeByRule()` (~1ms, 5 rules); if a rule decides, `propose_delegation` right there → approval card; if not, wake L4 (T2). 4 runaway guards (5 per day, 2 per thread per 24h, exclude `confidence < 0.70`, always exclude when `injection_flags` is present). One approval → run `delegate.run`; full autonomy only when the `settings` rules are enabled, and it still needs approval when `est_minutes > 30`, outside the repo, or involving egress | `packages/agents/src/delegate/{route,brief}.ts`, `packages/agents/src/loops/delegate.ts` | `pnpm --filter @omnis/agents test` | Opus | B07 | agents |
| US-B21 | L7 note routing loop (A4 §8): `note` insert trigger → at most 3 candidates (`propose_route`), if confidence is low it proposes nothing at all (converging on manual selection), no automatic routing — `notes.route_state` becomes `accepted` only once a human accepts | `packages/agents/src/loops/note-route.ts` | `pnpm --filter @omnis/agents test` | Sonnet | B07 | agents |
| US-B22 | L6 Network follow-up loop (A4 §7): first-contact detection (§7.2), inactivity sweep (`network_inactive_sweep`, weekdays 10:00, SQL based on `persons.cadence_days`/`priority_score`), output = tone-matched draft + task, update `persons.relationship_state` (only `closed` needs approval) | `packages/agents/src/loops/followup.ts` | `pnpm --filter @omnis/agents test` | Sonnet | B03, B07 | agents |
| US-B23 | L5 morning briefing (A4 §6.1~§6.3): synchronous call at 06:30 KST (not a batch queue), content model with 6 sections + `BriefItem`, **ranking is an arithmetic score, not an LLM** (weighted sum of 8 terms; a thread appears at most once in the whole briefing), the LLM writes only one-line summary sentences. One `digests(kind='morning')` row + automatically computed coverage metric | `packages/agents/src/loops/digest-morning.ts`, `packages/agents/src/digest/rank.ts` | `pnpm --filter @omnis/agents test` | Opus | B18, B19 | agents |
| US-B24 | L5 nightly digest (A4 §6.4·§6.5): 23:00 KST, `NightlyDigest` model (per-group `reason`/`samples`≤3/`undo_token` valid 7 days), exposes **all** auto-archived items, `cost` field (MTD/cap/tier_state), nightly memory consolidation is T2 + Anthropic Message Batches (23:30 `memory_consolidate`, harvested before the next morning). Whether a DeepSeek Batch API exists is left open as S-A4-2 | `packages/agents/src/loops/digest-nightly.ts`, `packages/agents/src/memory/consolidate.ts` | `pnpm --filter @omnis/agents test` | Opus | B14, B18, B23 | agents |
| US-B25 | self-model patch proposals (A4 §13): Sunday 21:00 job, 5 inputs (diffs of drafts I edited and sent myself, hand-corrected labels, new entities contradicting the self-model, the full file text, whether a cap is exceeded), patch constraints (1 per file·max 3·changes ≤20 lines·`evidence` ≥2; deletions from USER.md need ≥3), on approval `git apply` + commit + invalidate the snapshot cache, ignored patches suppressed for 4 weeks by diff hash | `packages/agents/src/self-model/propose.ts`, `packages/kernel/src/self-model/apply.ts` | `pnpm --filter @omnis/agents test` | Opus | B02, B07 | agents |
| US-B26 | Unified search API (A4 §14): `GET /search?q&k&scope&since`, four parallel query branches (items FTS + trgm fallback / threads aggregation / persons trgm+handle_norm / memories kNN), within-group normalization + `group_weight` merged ranking + recency·VIP bonus, cap of 5 per group and 20 overall, `SearchResponse` schema unchanged. Does not leave an `agent_runs` row (a search a human typed is not an agent run) | `apps/hub/src/search.ts`, `packages/memory/src/search.ts`(modified) | `pnpm --filter @omnis/hub test` | Opus | B01, B03 | surfaces |
| US-B27 | ⌘K search mode (A5 §2.5): when the input matches no action, the palette switches to search results, fixed group order (people→threads→items→memories), 180ms debounce, memories whose `deep_link` is null are not clickable, empty-state·slow-state copy | `packages/ui/src/components/command-palette.tsx`(modified), `apps/desktop/src/api/search.ts` | `pnpm --filter @omnis/desktop test` | Sonnet | B26 | surfaces |
| US-B28 | Today screen (A5 §3.4): greeting `<h1>`, nightly digest entry card (only when a nightly exists), today's calendar, morning briefing list (click an item → thread deep link), pending-approval chip strip (click expands inline — no navigation). All 4 states (loading/empty/error/offline) | `apps/desktop/src/screens/Today.tsx`, `packages/ui/src/components/digest-card.tsx` | `pnpm --filter @omnis/desktop test` | Sonnet | B23 | surfaces |
| US-B29 | Tasks screen (A5 §3.5): 4 view tabs (Today/This week/Someday/Delegated), `TaskRow` (native checkbox + source deep link + `kind` icon + dashed underline for `due_basis='inferred'`), quick add via the `t` shortcut, Delegated rows → navigate to the Agent Session, delegation approval card inline | `apps/desktop/src/screens/Tasks.tsx`, `packages/ui/src/components/task-row.tsx` | `pnpm --filter @omnis/desktop test` | Sonnet | B19, B20 | surfaces |
| US-B30 | Network screen (A5 §3.6): `PersonCard` (initials avatar per B-D3, affiliation·title from the `entities` "as of now" view, 3-level relationship dot alongside a text label), follow-up queue strip at the top, person detail pane (timeline + all-channel links + notes), "this is the same person" merge/split UI | `apps/desktop/src/screens/Network.tsx`, `packages/ui/src/components/person-card.tsx` | `pnpm --filter @omnis/desktop test` | Sonnet | B03, B22 | surfaces |
| US-B31 | Notes screen (A5 §3.7): one-line input (global `n` shortcut, focus retained after save), `RoutingSuggestion` with 3 buttons (accept / other target / do not route), confidence shown as text only ("high"/"low"), errors and no-match are not presented to the user as distinct cases | `apps/desktop/src/screens/Notes.tsx` | `pnpm --filter @omnis/desktop test` | Sonnet | B21 | surfaces |
| US-B32 | Digest screen (A5 §3.8): category accordion, restore a group or an individual item (optimistic update + toast), monthly cost report section, the "Auto-archived · N days ago — Restore" inline banner in the Thread header (within 7 days only) | `apps/desktop/src/screens/Digest.tsx` | `pnpm --filter @omnis/desktop test` | Sonnet | B24 | surfaces |
| US-B33 | Settings screen (A5 §3.9) + `settings` write path: 4 sub-navs (Accounts/Autonomy/Model tiers/General), an **editable** cost cap number input + progress bar (final 10% reserve segment, 80%/100% colors alongside text), autonomy allow toggle (off by default + warning dialog), local·Drive·GitHub allowlist editing, auto-archive threshold, quiet hours, 2-step kill switch confirmation | `apps/desktop/src/screens/Settings.tsx`, `apps/hub/src/settings.ts` | `pnpm --filter @omnis/desktop test && pnpm --filter @omnis/hub test` | Sonnet | B09, B11, B14, B18 | surfaces |
| US-B34 | Tailscale Serve mounts + ACL docs: `/api/` → `localhost:8787`, `/` → the PWA static build, a `*.ts.net` certificate check script, the ACL (grants) doc states why Postgres 5432·Ollama 11434 are never put in `dst`, Funnel is always OFF | `ops/mini/tailscale-serve.sh`, `ops/mini/TAILSCALE-ACL.md` | `bash ops/mini/tailscale-serve.sh --check` | Sonnet | — | ops |
| US-B35 | `apps/web` PWA shell (A5 §4.1~§4.3, B-D4): Vite + the same `@omnis/ui`, 5-slot bottom tab bar (Inbox/Today/Tasks/Network/Notes — Digest·Settings are not tabs), manifest + service worker + 3-step install guide card, swipe actions (right = Archive, left = action menu, pending approvals use a tap instead of a swipe), 4 snooze presets, 3 quick-reply chips, the hub serves the build output | `apps/web/src/**`, `apps/web/public/manifest.webmanifest` | `pnpm --filter @omnis/web test` | Sonnet | B28, B34 | surfaces |
| US-B36 | PWA Web Push subscription (A5 §4.4): the permission request happens **when the first pending-approval item appears**, in context; `POST /push/subscribe` + store in `push_subscriptions`, service worker `notificationclick` (Approve = accept `pending_approvals` without opening the app, Open = deep link), copy for 6 notification kinds | `apps/web/src/push/*.ts`, `apps/hub/src/push.ts` | `pnpm --filter @omnis/web test` | Sonnet | B17, B35 | surfaces |
| US-B37 | Outlook adapter (A1 §2.4): `/common` authority OAuth (3 delegated scopes), delta polling (`/me/mailFolders/inbox/messages/delta`), thread=`conversationId`·item=`id`·`sourceHash=internetMessageId`, 3 write-backs (send/markRead/archive=move), 429 `Retry-After`, delta 410 → full resync. **Accepted via fixture contract tests only** (B-D5) | `packages/adapters/outlook/src/index.ts`, `packages/adapters/outlook/fixtures/*.json` | `pnpm --filter @omnis/adapter-outlook test && pnpm test:contract` | Sonnet | — | channels |
| US-B38 | Telegram adapter (A1 §2.5): mtcute sidecar, QR-login pairing flow, session file + Keychain wrapping key, MTProto update stream, `getHistory` backfill, write-back (send/markRead, archive=false), dynamic flood-wait. **Accepted via fixture contract tests only** | `packages/adapters/telegram/src/index.ts`, `packages/adapters/telegram/fixtures/*.json` | `pnpm --filter @omnis/adapter-telegram test && pnpm test:contract` | Sonnet | — | channels |
| US-B39 | Hermes read-only session (A2 §4.4, B-D7): an HTTP-style runtime adapter in `local-agent` (`base_url` defaults to `http://127.0.0.1:8642`, bearer), `session_key` → `X-Hermes-Session-Key` 1:1, `/v1/responses` chain, SSE keepalive is not raised as an event and only feeds the health timer, only `origin:'human'` is allowed (`delegation` rejected), separate `AgentRuntime` registration for the mini and the MacBook | `apps/local-agent/src/bridges/hermes.ts` | `pnpm --filter @omnis/local-agent test` | Opus | — | channels |
| US-B40 | Real-connection hardening + adapter health → system Item: the token refresh job (`token_refresh`) actually runs, re-subscribe `gmail_rewatch` (7 days)·`graph_sub_renew` (10,080 minutes), backfill caps (30 days·500 items per channel), on `auth_required`/`broken` transitions set `accounts.state`+`last_error`+a system Item, N consecutive `health()` failures → ntfy + inbox dual surfacing. All accepted with a mock server | `packages/kernel/src/jobs/{token-refresh,rewatch}.ts`, `packages/kernel/src/adapter-health.ts` | `pnpm --filter @omnis/kernel test:integration` | Sonnet | B37, B38 | channels |
| US-B41 | Backup: `pg_dump --format=custom` 03:00 LaunchDaemon + restic → B2 (`pg` dump + self-model git + `secrets/*.enc.yaml`), forget policy (7 days/4 weeks/6 months), **quarterly restore rehearsal script** (restore to scratch port 5433 + row counts for `items`/`threads`/`pending_approvals` + verify the latest `sent_at`) + `backup/restore-drills.md` | `ops/scripts/omnis-backup.sh`, `ops/scripts/restore-drill.sh`, `ops/mini/LaunchDaemons/*.plist` | `bash ops/scripts/restore-drill.sh --dry-run` | Sonnet | — | ops |
| US-B42 | Monitoring + log rotation: ping wiring for 15 healthchecks.io checks (success `/`, failure `/fail`), self-hosted ntfy with 2 topics (`omnis-critical`/`omnis-warning`), every critical/warning surfaces to both ntfy + `items(kind='system')`, `newsyslog`/logrotate 30-day retention + verify secret masking | `ops/scripts/healthcheck-ping.sh`, `ops/mini/newsyslog.d/omnis.conf` | `bash ops/scripts/healthcheck-ping.sh --check` | Sonnet | B40 | ops |
| US-B43 | Mini boot checklist automation (A6 §2): verify auto-login with FileVault on, `pmset` settings (no sleep·auto boot on power restore), LaunchDaemon/Agent load state, Ollama models present, one slot health check — a single script checks everything and prints only the failures | `ops/mini/preflight.sh`, `ops/mini/RUNBOOK.md`(modified) | `bash ops/mini/preflight.sh --check` | Haiku | — | ops |
| US-B44 | Monthly cost·usage report job: aggregate `agent_runs` (tokens·cost·cache hit rate `tokens_cached/tokens_in` by loop·tier·provider), load into `digests.metrics` on the 1st of the month + the nightly digest `cost` field surfaces MTD daily, loops with a cache hit rate below 40% get a warning line in the report | `packages/kernel/src/jobs/cost-report.ts` | `pnpm --filter @omnis/kernel test:integration` | Sonnet | B14, B24 | ops |
| US-B45 | Hub adapter registry + bootstrap wiring: `apps/hub/src/adapters.ts` reads the `accounts` (+ `account_secrets.auth_ref`) rows and uses each channel adapter package's factory to build `Adapter` instances and `connect(AuthRef)` (each adapter reads its own secrets straight from the Keychain — the hub passes only the item name), injects them into `createHubServer({adapters})` so the archive write-back (US-A36) and later send paths actually work, and starts a `subscribe()` loop per `state='active'` account that records health transitions as system Items. The registry (`AdapterFactories`) is injectable, so **fixture tests run with fake factories** (B-D5) | `apps/hub/src/adapters.ts`, `apps/hub/src/main.ts`(modified) | `pnpm --filter @omnis/hub test` | Opus | B37, B38, B40 | channels |

---

## 3. The 5 plan files

| Plan file | Slug | Stories | Count |
|---|---|---|---|
| `2026-09-20-phase-b-memory-ingestion.md` | `memory-ingestion` | US-B01, B02, B03, B04, B05, B08, B09, B10, B11, B12 | 10 |
| `2026-09-20-phase-b-agents.md` | `agents` | US-B06, B07, B13, B14, B15, B17, B18, B19, B20, B21, B22, B23, B24, B25 | 14 |
| `2026-09-20-phase-b-surfaces.md` | `surfaces` | US-B26, B27, B28, B29, B30, B31, B32, B33, B35, B36 | 10 |
| `2026-09-20-phase-b-channels.md` | `channels` | US-B37, B38, B39, B40, B45 | 5 |
| `2026-09-20-phase-b-ops.md` | `ops` | US-B16, B34, B41, B42, B43, B44 | 6 |

**Parallel wave proposal** (worktrunk sibling worktrees, one branch per plan `plan/<slug>`):

- **W1**: memory-ingestion US-B01~B05 + all of ops (US-B16/B34/B41~B43 have no code dependencies) + channels US-B37~B39
- **W2**: after agents US-B06~B07 merges → US-B13~B25, memory-ingestion US-B08~B12
- **W3**: all of surfaces + channels US-B40 + ops US-B44

---

## 4. Prohibitions common to all stories

It inherits A7 §7's 4 Phase A prohibitions verbatim, and Phase B adds 5 more.

**Inherited**:
- Do not wire irreversible tools (`send`/`delete`/`delegate`/`calendar_write`) directly outside an approval gate.
- Do not import provider SDKs arbitrarily outside `packages/protocol` (keep them isolated inside adapters and `t1/provider.ts`).
- Do not edit migration files — always append a new number (append-only).
- Do not delete or skip tests to make them pass.

**Added by Phase B**:
1. **Do not import egress modules from `packages/agents/**`** (A4 §1.1). It is forbidden even inside `apply()`, and enforced by a lint rule (US-B07).
2. **`propose_*` only stores.** Not even `archive` is a model tool — auto-archiving is entirely SQL in a kernel job (A4 §9).
3. **Do not create a hard-delete path.** `memories`/`relations` are invalidated only via `invalidated_at`, and `items` are kept forever (A3 §11).
4. **Do not open paths matching an A4 §10.2 exclusion pattern.** Never decide by looking at the contents — to decide you would already have read it. One violation = CI failure (US-B12).
5. **Do not put real account credentials in tests** (B-D5). Adapters, ingestion, and push are all accepted with fixtures/mocks.
6. Do not read subscription credentials such as `~/.claude/.credentials.json` (A4 §12.1 T3 boundary; Phase A tests already watch for this).

---

## 5. Phase B exit criteria (master §16)

> Start measuring draft adoption rate·briefing coverage; measure real monthly cost.

Concretely, what "measurable" means:

| Metric | Target | Measurement path | Owning story |
|---|---|---|---|
| Draft adoption rate | ≥ 50% | `items.status` transitions (`draft`→`approved`/`sent`) | US-B13 |
| Send-without-edit rate | ≥ 20% | Levenshtein between `sent` body and `draft` body | US-B13 |
| Briefing coverage | ≥ 80% | `digests.item_ids` ∩ items opened or replied to that day | US-B23 |
| Auto-archive misfires | precision ≥ 0.97, **0** VIP·sensitive archives | 150 cases in `eval/auto_archive.jsonl` + CI hard gate | US-B18 |
| memory recall@10 | ≥ 0.80 | 50 cases in `eval/memory_recall.jsonl` | US-B12 |
| Monthly cost | The measured value comes from the sum of `agent_runs.cost_usd` (estimated ~$22, cap $60) | `cost_daily` view + monthly report | US-B14, US-B44 |
| Cache hit rate | draft loop `tokens_cached/tokens_in` ≥ 40% | weekly report | US-B44 |
| Injection defense | all 20 test cases pass | CI | US-B05 |

**Phase B entry spikes** (this Phase's share of A6 §11.2's "16 at phase entry"): `A1-⑥` (Outlook Graph delta), `A1-⑦` (Telegram mtcute pairing), `S-A4-2` (whether a DeepSeek Batch API exists), `S-A4-6` (GitHub App vs PAT), `S-A4-7` (search p95 latency), **`gate-hermes-sse` (Hermes `/v1/responses` SSE event field names — newly registered in the 2026-09-20 cross review, owned by channels plan Task 11-S)**; `S-A2-5` is deferred to Phase C. All run inside their story, and each records pass/fail in `tools/spikes/<slug>/result.md`.

---

## 6. Open questions

1. **Updating the master §16 table.** The 4 items pulled forward in §1 (todos·delegation, Network·follow-up, note routing, person label) are still sitting in the Phase C row. Logan needs to decide whether to edit the master or label this backlog "Phase B+".
2. **The `settings` table (B-D2) is a new table that A3 does not have.** It conflicts with A3 §8's "this list is the complete list of v1 tables" — `0009` must be added to A3 (delta document §5 has a draft).
3. **The reference time for the auto-archive `undo` window** lives in `items.meta.archived_by.at` (jsonb), so a single day's query cannot use an index (A4 §9.3 left it open as "promote to a column if it gets slow"). After 2 weeks of measurement, decide whether to promote it to an `items.archived_at` column.
4. **iOS background reliability of Web Push** (A5 §4.4, `13`). Record the delivery-miss rate as an instrumented metric in US-B36; if it recurs, use it as grounds for a native shell in v2.
5. **The 40-case draft golden set** (`eval/draft.jsonl`) is graded against "replies Logan actually sent", so **it cannot be filled before real account connection**. US-B13 only wires up the tone-similarity·length-compliance metrics; the set is filled after connection. → **Decided (§7-1)**.
6. **The Anthropic API key for `memory_consolidate` (T2 Batch)** is not in the Keychain yet (`omnis.anthropic.api_key`). Without the key, US-B24's consolidation job is skipped and only the digest runs — need to confirm whether this fallback should be the default behavior.

---

## 7. Logan's decisions (after the 2026-09-20 cross review)

These are the answers to the 3 items the cross review (`2026-09-20-phase-b-plans-review.md` §4) raised. This section is canonical, and the corresponding items in §5·§6 above point here.

1. **The golden-set exit criteria are deferred until real account connection.** For `eval/draft.jsonl` (B13)·`eval/task.jsonl`·`eval/route_note.jsonl`·`eval/followup.jsonl`, synthetic data is accepted for **the format, the harness, and the hard gates only**, and the three metrics in the §5 table — **draft adoption rate (≥50%)·send-without-edit rate (≥20%)·briefing coverage (≥80%)** — are **excluded** from the Phase B exit decision (Phase B's exit condition is reached once the measurement wiring runs). What is **not** deferred: **0 VIP·sensitive archives** in `eval/auto_archive.jsonl` and **recall@10 ≥ 0.80** in `eval/memory_recall.jsonl` — the former is a safety invariant that must not break even with synthetic data, and the latter's ground truth is the source documents, so synthetic data still yields the real value.
2. **Hub·local-agent bootstrap wiring is US-B45 in the channels plan.** The story was newly added in §2 above (`apps/hub/src/adapters.ts` + edits to `apps/hub/src/main.ts`). The adapter factory registry is made injectable so fixture tests run with fake factories (B-D5 preserved). The `adapters: Map<RuntimeKind, RuntimeAdapter>` wiring in `apps/local-agent` is closed in the same US-B39 task that attaches the Hermes runtime.
3. **Register the Hermes SSE field-name spike as a Phase B entry spike.** Slug `gate-hermes-sse`, owned by the US-B39 prerequisite task in the channels plan (Task 11-S). The mini's Hermes `api_server` (:8642) may not be running, so **if there is no live connection, proceed from documentation + fixtures and state `UNVERIFIED` explicitly in `result.md`** — a failing spike does not block US-B39 (the mechanism is fully verified with mock SSE; only the field names remain to be pinned down at real-connection time).
