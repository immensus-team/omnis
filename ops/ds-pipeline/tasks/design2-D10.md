# D10 (design wave 2) — Detail pane: rounded card, collapse/expand button, drag-to-resize divider

Worktree: /Users/logankim/AI-Workspaces/omnis.plan-design-w2 (branch plan/design-w2). Per-branch DB: omnis_test_design_w2. Authority: docs/design/DESIGN-DIRECTION-v3.md (§c.1 pointer-drag primitive, §c.5 DetailView, §b tokens) + DESIGN-DIRECTION.md + ACCENT-DIRECTION.md. Seeded screens via tools/e2e (shots.ts / shots-responsive.ts). English only.

Logan's direction (his screenshot: the right-hand detail pane with the approval card is a flat, square-cornered panel on the canvas): (1) make the detail pane a ROUNDED floating card like the inbox list card (same radius/elevation tokens as `.inbox-card`, opaque white, no hairline border, soft shadow), with the approval card and thread content inside it; (2) make it collapsible/expandable with a button, like every mainstream AI app's sidebar: a chevron toggle in the pane header (and `]` / `[` or ⌘\ keyboard shortcut — pick what use-keymap.ts already reserves), spring-animated open/close (240ms tier, reduced-motion → instant), collapsed = the pane disappears and the list takes the width (the thread then opens as the D2b sheet/overlay when a row is clicked); (3) a DRAG HANDLE on the pane's left edge (his second screenshot: a short pill-shaped grip centered vertically on the divider, visible on hover/focus, cursor col-resize) that resizes the pane between 320px and 50% of the shell with fluid, water-like motion (rubber-band past the limits, spring settle on release), double-click resets to the default width, keyboard: focus the handle + arrow keys resize in 16px steps; (4) persist width and collapsed state in settings (`ui.detail_width`, `ui.detail_collapsed`) via the existing settings API, restore on load; (5) below 900px (single/two-pane tiers) the handle and toggle are hidden — the sheet behavior stays. Use the pointer-drag primitive from v3 §c.1 (shared with the rail reorder) rather than a new dependency. Tests: RTL for toggle/keyboard/persistence and the drag math (min/max/rubber-band); Playwright screenshots docs/design/screens/wave2/detail-pane-{open,collapsed,resized}-1440.png and 390.png; no horizontal overflow at any width. Commit "US-D10: detail pane — rounded card, collapse toggle, drag-to-resize divider" with the trailers.

Acceptance: pane reads as a rounded card consistent with the list card; toggle + shortcut work with spring motion; handle resizes with clamping and rubber-band; width/collapsed persist across reload; screenshots committed and show the three states; lint/typecheck/tests green; tree clean.
