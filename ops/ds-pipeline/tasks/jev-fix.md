# Jev spike follow-up: seed the new setting key with a NEW migration (main gate fails)

Worktree: /Users/logankim/AI-Workspaces/omnis.plan-jev (branch plan/jev). Per-branch DB: omnis_test_jev. First `git merge main` (main moved: W4b screens merged) and resolve conflicts keeping both.

Failure on main after merging this branch: `packages/kernel/test/integration/settings.test.ts > 0009_settings.sql seed > seeds every SettingKey except the internal cost.last_state` fails because the spike added the `agents.decision_provider` SettingKey without a seed row. Applied migrations 0001–0013 are FROZEN (repo rule in CLAUDE.md) — do NOT edit 0009. Add `packages/db/migrations/0014_agents_decision_provider.sql` inserting the seed row (`agents.decision_provider` = "llm") with the same idempotent pattern the other seed migrations use, register it wherever the migration list/runner requires, and adjust the settings test only if its assertion is about "the seed after all migrations" (it should keep asserting every SettingKey has a row). Run the FULL `pnpm test` (not just agents) plus lint/typecheck on the per-branch DB; all green. Commit "US-JEV: seed agents.decision_provider via migration 0014" with the trailers. Acceptance: full suite green on the merged tree; 0009 byte-identical to main; migration numbering contiguous.
