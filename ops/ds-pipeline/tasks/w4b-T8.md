# W4b — surfaces plan Task 8: Settings screen (screen only) (US-B33)

Worktree: /Users/logankim/AI-Workspaces/omnis.plan-w4b (branch plan/w4b). Per-branch DB: omnis_test_w4b. Plan: docs/superpowers/plans/2026-09-20-phase-b-surfaces.md — read the section "### Task 8:" in full, plus the plan header / Global Constraints, docs/superpowers/plans/2026-09-20-phase-b-interfaces-delta.md, the backlog row US-B33 in docs/superpowers/plans/2026-09-20-phase-b-backlog.md, and the review notes for US-B33 in docs/superpowers/plans/2026-09-20-phase-b-plans-review.md. Earlier waves landed the real names (W4a: GET /search, GET /transcript/:id, settings module + GET/PUT /settings, GET /cost; W3 loops; W5 registry) — grep for them first and adapt the plan's names; record deviations in the commit body.
Scope: the Settings SCREEN only — the kernel settings module and hub GET/PUT /settings landed in W4a; reuse them. Hide cost.last_state (internal key). Include the rail order setting (ui.rail_order) if design wave 2 has landed it; otherwise leave a TODO note in deviations, not code.
UI rules: the plan's UI copy is Korean — ship natural English copy instead; reuse packages/ui components (StatusPill, GroupHeader, FilterChipBar, KeyValueTable, PersonCard, ChannelGlyph, GlassSurface, AuroraSurface, SegmentedControl, ApprovalStack); follow docs/design/DESIGN-DIRECTION.md (+ DESIGN-DIRECTION-v3.md if present: monotone paper canvas, glass only on chrome) and docs/design/SKILLS.md checklist; the screen must work at 1440 (three panes) and 390 (single pane + bottom bar) with no horizontal overflow. Screens: commit docs/design/screens/w4b/<screen>-{1440,390}.png shot from the seeded app (tools/e2e/shots.ts pattern).

Acceptance: every checkbox of the plan task done (tests included and green on the per-branch DB; `pnpm lint && pnpm typecheck` clean); English only; screenshots committed and, when Read, showing a finished screen with real seeded data (no empty placeholder); commit per the plan's commit step with the trailers.
