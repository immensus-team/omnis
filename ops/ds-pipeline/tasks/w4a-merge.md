# Merge Phase B W4 part a into main

Repo: /Users/logankim/AI-Workspaces/omnis. Branches, in order: plan/w4a-search, plan/w4a-transcript, plan/w4a-settings, plan/w4a-cost. For each: `export PATH=/opt/homebrew/bin:$PATH && git pull -q --ff-only origin main && git merge --no-ff plan/<b> -m "merge: plan/<b> (Phase B W4a)

Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>"`; conflicts in packages/kernel/src/index.ts, apps/hub/src/http.ts, tsconfig references, pnpm-workspace: append-only unions (keep both); lockfile → `pnpm install`. Then with `PGUSER=logankim DATABASE_URL=postgres://logankim@127.0.0.1:5432/omnis_test`: `pnpm install && pnpm db:migrate && pnpm typecheck && pnpm lint && pnpm test`, then `pnpm e2e:phase-a` (commit refreshed evidence "e2e: refresh after Phase B W4a" if it changed). Fix failures with minimal "fix: post-merge — …" commits. Push origin main. Redeploy the Mac mini per ops/mini/RUNBOOK.md (rsync, pnpm install --frozen-lockfile, build, db:migrate, launchctl kickstart -k com.omnis.{hub,zero-cache,local-agent}; verify https://your-hub.your-tailnet.ts.net/health returns ok). Remove the four worktrees (`git worktree remove …`) and drop omnis_test_w4a_{search,transcript,settings,cost}. Print: main sha, tests, e2e, mini health, notes.
