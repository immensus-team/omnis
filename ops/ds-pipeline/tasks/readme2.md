# README — make it concise (Logan: "too long")

Worktree: /Users/logankim/AI-Workspaces/omnis.plan-readme2 (branch plan/readme2). No DB. Only file: README.md (currently ~220 lines on main). Run `pnpm install` first if node_modules is missing (lint/typecheck are irrelevant to this task but the gate runs them).

Target: ≤ 110 lines, same Hermes-Agent structure and voice, nothing invented. Keep, verbatim where marked: banner image; `# omnis` + tagline line **Inbox that works with you.**; the links line and the badge rows (shields, flat-square); the category pitch paragraph (**omnis is the inbox where all communication between people and agents happens.** … one real-time stream, one memory, one place to decide.) and the 3-sentence "works with you" paragraph; the "Use any model you want" paragraph shortened to 2 sentences; the three theses verbatim (3 lines). Then: "What it does" as ONE compact table or 8 bullets max (unified inbox with AI summary rows · agent sessions as threads with approvals and kill switch · drafts in your voice · archive + nightly digest · tasks and delegation · people cards · memory across sessions · ⌘K search); "How it works" = one mermaid diagram (≤ 12 nodes) + 3 lines of principles; "Quickstart" = 3 fenced bash steps, ≤ 3 commands each; "Status" = 3 lines with the real numbers already in the README; "Docs" = 4 links; "License" = 1 line. Remove everything else (long tables, per-channel prose, repeated caveats — one honest sentence that live accounts are not connected yet is enough). No new claims; every link/path must exist; English only.

Commit "docs: README — concise pass" with the trailers. Acceptance: `wc -l README.md` ≤ 110; the verbatim blocks above are intact; every relative link resolves (`grep -o '](\([^)]*\))' README.md`); mermaid block valid; reads like Hermes Agent's README, not a spec.
