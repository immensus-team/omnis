# D3: Detail pane and person card refinement

Worktree: /Users/logankim/AI-Workspaces/omnis.plan-design-w1 (branch plan/design-w1). Per-branch DB: omnis_test_design_w1. Desktop dev server: `pnpm --filter @omnis/desktop dev` (Vite 5173). For realistic screens use the e2e seed: read tools/e2e/run.ts / tools/e2e/seed.ts / tools/e2e/shots.ts and reuse them (they boot hub + zero-cache + seed omnis_e2e). Playwright is installed (tools/e2e). Project skills live in .claude/skills (hallmark, motion-dev-animations, design-tokens, tailwind-v4-shadcn) and load via the Skill tool. Screenshots go under docs/design/screens/ and are committed.

Hard rules: glass only on rail/toolbar/sheet/palette/floating panels; lists/body opaque; light theme; no new heavy dependencies without checking package.json; never hand-draw brand SVGs; reduced-motion respected; nothing overflows horizontally at 390/768/1024/1440.

## Brief
Reference: docs/design/reference/ref-dashboard-detail-card.webp. (1) Thread detail header: title + subline (channel · participants · last activity), segmented control for "대화 / 요약 / 메모", right-aligned icon actions (보관, 라벨, 더보기). (2) A key-value hairline table component (packages/ui KeyValueTable: 13px labels left in grey, values right, 1px hairline rows — the ONE place hairlines are allowed) used for thread meta and for the person card (name, channels, labels, last contact, relationship state). (3) Person card (used by hover card and the future Network screen) with avatar/initials, badge row, KeyValueTable. (4) Typography pass per reference: 15/13/12 scale, tabular numbers for times, tracking on captions. (5) Approval stack in the detail pane: today every pending approval renders as an identical card wall (same radius/border/shadow/four buttons) — give it hierarchy: only approvals belonging to the OPEN thread, newest/riskiest one expanded and elevated, the rest collapsed to one-line rows with a count header; also vary the e2e fixture copy in tools/e2e/shots.ts densify() so screenshots stop repeating one string three times. (6) Tests: KeyValueTable rendering, person card, approval stack scoping/collapse; screenshot docs/design/screens/detail-pane.png. Commit "US-D03: detail pane header, key-value table, person card".

## Acceptance (the reviewer will check all of these)
- Every item of the brief implemented; tests named in the brief exist and pass; `pnpm lint && pnpm typecheck` clean; the named screenshots committed and, when Read next to the reference images, actually read as the reference.
- Commit messages as the brief says, with the trailers.
