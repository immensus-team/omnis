# D2b: Official brand marks + responsive layout that never breaks

Worktree: /Users/logankim/AI-Workspaces/omnis.plan-design-w1 (branch plan/design-w1). Per-branch DB: omnis_test_design_w1. Desktop dev server: `pnpm --filter @omnis/desktop dev` (Vite 5173). For realistic screens use the e2e seed: read tools/e2e/run.ts / tools/e2e/seed.ts / tools/e2e/shots.ts and reuse them (they boot hub + zero-cache + seed omnis_e2e). Playwright is installed (tools/e2e). Project skills live in .claude/skills (hallmark, motion-dev-animations, design-tokens, tailwind-v4-shadcn) and load via the Skill tool. Screenshots go under docs/design/screens/ and are committed.

Hard rules: glass only on rail/toolbar/sheet/palette/floating panels; lists/body opaque; light theme; no new heavy dependencies without checking package.json; never hand-draw brand SVGs; reduced-motion respected; nothing overflows horizontally at 390/768/1024/1440.

## Brief
Logan's screenshot (narrow window) showed: filter chips wrapping onto three lines ("Archived" tab broken into two lines, "+ Label" alone on a row), row titles wrapping to two lines with the time and brand glyph drifting, and rail tiles using monochrome/generic glyphs. FIRST: `git merge main` into this branch (brings CLAUDE.md = English-only rule and the latest code; resolve conflicts keeping both sides; run tests). ENGLISH ONLY from here: every UI string, comment and test name you add is English; rename the Korean strings you touch (e.g. "보관됨"→"Archived", "+ 라벨"→"+ Label", aria-labels "채널"→"Channels", "더 보기"→"More"). (1) Brand marks — Logan supplied the exact logo images to use; they are in /Users/logankim/AI-Workspaces/Claude/omnis/brand-assets/ (<name>@1x.png = 64px, <name>@2x.png = 128px; originals in src/): slack, gmail, gcal (Google Calendar), outlook, telegram, whatsapp, kakaotalk, linkedin, agent (the black spy/detective silhouette = the Agents rail tile AND the generic agent channel mark), system (gear + magnifier = the 'system' channel). Copy the @1x/@2x PNGs into packages/ui/src/assets/brands/ and render them through ChannelGlyph as <img srcSet="…@1x.png 1x, …@2x.png 2x" width/height fixed, alt="" aria-hidden> — keep ChannelGlyph the single rendering point (CHANNEL_ICON map → asset map), sizes 18px rail / 16px row / 20px detail; the images already carry their own tiles/rounded corners so REMOVE the KakaoTalk yellow tile wrapper (CHANNEL_TILE_BG) and any tinting (CHANNEL_COLOR must not be applied to images). Use these images verbatim — never hand-draw or recolor a brand mark. Agent runtime marks (Claude Code / Codex / DeepSeek / Hermes) for runtime badges: keep the existing react-icons ones (SiAnthropic, SiDeepseek; add SiOpenai for codex) and for Hermes a neutral lucide glyph — do not fetch anything from the network. Read docs/design/reference/kinso-inbox.webp: tile size 40px, radius 12px, mark 18–20px centered. (2) Responsive layout with container queries (Tailwind v4 / native CSS @container on the list pane and the shell): shell breakpoints — ≥1280px three panes (rail 76 / list / detail), 900–1279 two panes (rail / list; the thread opens as a right-side glass sheet), <900 (incl. iPhone PWA 390px) single pane with the rail collapsed to a 56px bottom bar (5 tiles + More) and the ask bar full-width. List pane rules at any width: filter tabs + label chips in ONE horizontally scrollable line (mask-image fade at both edges, no scrollbar, never wraps), "+ Label" and "Archived" become icon buttons under 560px container width with tooltips; row grid `40px minmax(0,1fr) auto` where title is one line with text-overflow ellipsis, summary clamps to 2 lines (1 line under 480px), time is tabular-nums fixed-width right-aligned, brand glyph fixed 20px column, label chips truncate to 2 + "+N"; the ask bar keeps min-width 0 and the avatar hides under 420px. Nothing may overflow horizontally: at every width `document.documentElement.scrollWidth <= clientWidth`. (3) Tests: RTL for chip-bar single-line class + icon-button collapse; Playwright script tools/e2e/shots-responsive.ts that boots the seeded app and screenshots widths 390, 768, 1024, 1440 into docs/design/screens/responsive/{w}.png and asserts no horizontal overflow and chip row height ≤ 40px at each width. Read your four screenshots next to kinso-inbox.webp and fix what still looks off (max 3 rounds). Commit "US-D02b: official brand marks + responsive shell/list".

## Acceptance (the reviewer will check all of these)
- Every item of the brief implemented; tests named in the brief exist and pass; `pnpm lint && pnpm typecheck` clean; the named screenshots committed and, when Read next to the reference images, actually read as the reference.
- Commit messages as the brief says, with the trailers.

## Carried from the last review (attempt 2 was rejected on these two; attempt 3 left UNCOMMITTED edits in the worktree — inspect `git status`/`git diff` first, keep what compiles, commit, continue)
- apps/desktop/src/app.css `.inbox-row__action { opacity: 0 }` keeps the hover-only Archive/Restore button in layout, so every row reserves ~60–70px of the side column; hide it out of flow (e.g. absolutely positioned inside the side slot, or `display: none` until hover/focus-within) so the summary gets that width back.
- Under the narrow shell (<560px container) only `.filter-chip-bar__add` and `.inbox-card__archived-pill` get `min-height: 32px`; the five primary view pills (all / work / personal / agents / needs-approval) must reach the same 32px tap target.
- Re-shoot docs/design/screens/responsive/{390,768,1024,1440}.png WITHOUT a label filter applied (a second `-filtered` set may stay) so the list shows many rows.
