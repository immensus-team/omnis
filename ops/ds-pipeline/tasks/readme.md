# README rewrite — "Inbox that works with you", Hermes-Agent style

Worktree: /Users/logankim/AI-Workspaces/omnis.plan-readme (branch plan/readme). No DB needed. Files: README.md (rewrite), docs/assets/omnis-banner.png (already committed on main — the hero banner: pixel-art trumpeting angels + "OMNIS — INBOX THAT WORKS WITH YOU"; Read it), optionally docs/assets/ for a light/dark variant is NOT needed.

## Positioning (Fable — use this; do not water it down; no parenthetical feature lists in the pitch)
Logan's direction: make it read like a global open-source project with a big idea, not a feature list. The pitch is the category, not the channel list.
- Tagline (banner + H1 subline): **Inbox that works with you.**
- Category sentence (open the pitch with it, in bold): **omnis is the inbox where all communication between people and agents happens.** Human-to-human messages, human-to-agent requests, agent-to-human questions and approvals, agent-to-agent handoffs — one real-time stream, one memory, one place to decide.
- Follow with 2–3 sentences of what "works with you" means, in this register: it reads everything before you do and leaves a one-line summary on every thread; it drafts in your voice, files what does not need you, turns messages into tasks and briefs you every night; it remembers across sessions and machines; and nothing leaves without your approval. Then one sentence on where it runs: your own machine, over your own network, with any model you choose.
- Do NOT write "every channel (Slack, Gmail, …) and every agent (Claude Code, …)" in the pitch. Channel and agent names belong only in the "Channels & agents" table and the feature grid.
- The three theses (keep verbatim): 1. Everything is an inbox — a message from a person and a turn from an agent land in the same queue. 2. Context is the product — the inbox is the surface; the asset is one unified memory about you. 3. Agents act first, you decide — drafts, labels, tasks and delegation proposals are ready before you look; anything that leaves the system passes your approval.
- Model freedom paragraph (mirrors Hermes's "Use any model you want"): omnis routes work through a four-tier cascade — local models, DeepSeek, Claude, or your own subscription CLIs — under a monthly cost cap; switch per task, no code changes, no lock-in.
- Voice: confident, international, precise; short sentences; no hedging, no emoji spam, no marketing adjectives ("revolutionary", "seamless"). It should read like Hermes Agent's README, not like a product sheet.

## Style: mirror the Hermes Agent README (github.com/NousResearch/hermes-agent) structure — study its raw README first with WebFetch (https://raw.githubusercontent.com/NousResearch/hermes-agent/main/README.md); if the fetch fails, use this description: full-width banner image; H1 "omnis" with a small emoji; a centered line of two links (Docs | Design); two rows of shields.io badges in the flat-square "label | value" style (DOCS → docs/spec/00-omnis-design.md, LICENSE Apache-2.0, BUILT BY Onword Lab, STATUS Phase B, PLATFORM macOS · iPhone, SELF-HOSTED Mac mini · Tailscale); a bold-opening pitch paragraph; the "use any model" paragraph with the `omnis` CLI/`settings` mention; then sections with short punchy prose, bullet lists, a couple of small tables, one mermaid architecture diagram, a 3-step quickstart in fenced bash blocks, and a status/roadmap. Tone: confident, concrete, no marketing fluff, no emoji spam, no "🚀".

## Sections (in this order)
1. Banner, title, links line, badges, pitch, model-freedom paragraph.
2. "What omnis does" — feature grid as a 2-column table or bullets: unified real-time inbox with AI summary rows; work/personal filter; agent sessions as inbox threads (tool calls, cost, approvals, kill switch); drafts in your voice with approval; manual + nightly auto-archive with digest; tasks + cross-machine delegation; network (people cards, follow-up nudges); notes routing; memory (inbox + local files + Drive + GitHub) with bi-temporal entities; unified search (⌘K).
3. "How it works" — mermaid diagram: Mac mini hub (Postgres 17 + pgvector, own kernel: events/scheduler/approvals/kill switch/audit; Zero sync; hub HTTP/WS) ↔ clients (Tauri macOS app, iPhone PWA) ↔ local agents (bridge JSON-RPC/WS; Claude Code, Codex, claude-ds, Hermes) ↔ channels (adapters). Then a 5-line "Design principles" (kinso-style light UI, glass only on chrome, monotone base with aurora accents, spring motion with reduced-motion, no AI-slop).
4. "Channels & agents" — table from the current code: channel | status (adapter + fixtures ready; live auth pending) ; agent runtime | how it connects. Be honest: real accounts are not connected yet; everything runs on replayed fixtures and seeded e2e.
5. "Quickstart" — 3 steps from ops/mini/RUNBOOK.md and tools/e2e (mini: clone, pnpm install, db:migrate, LaunchAgents; app: pnpm --filter @omnis/desktop dev; verify: pnpm e2e:phase-a). Read those files so the commands are real.
6. "Status" — Phase A done (35 stories), Phase B W0–W5 merged (say what landed), W4b screens in progress; test counts from the latest run (read tools/e2e/REPORT.md and the last CI/test numbers in git log messages, e.g. "183 test files / 1453 tests"); link docs/superpowers/plans/README.md.
7. "Docs" — links: docs/spec/00-omnis-design.md, appendices A1–A8, docs/design/DESIGN-DIRECTION.md, docs/research/.
8. "Built with" — DeepSeek V4.1 Flash + Claude (Opus/Sonnet/Fable) agent workflows, one line, honest.
9. "License" — Apache-2.0 (LICENSE file exists).

Rules: English only; every command and path must exist in the repo (verify with ls/grep); no invented metrics; keep it under ~220 lines; no claims about connected accounts. Commit "docs: README — Inbox that works with you (Hermes-style)" with the trailers.

Acceptance: README renders (check with `npx markdownlint-cli2 README.md` if available, else eyeball); banner path correct; every relative link resolves (`grep -o '](\([^)]*\))' README.md` and test each with ls); badges are valid shields.io static URLs; mermaid block is syntactically valid; positioning + three theses present verbatim; honesty about status.
