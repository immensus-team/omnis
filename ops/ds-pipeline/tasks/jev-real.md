# Jev — real evaluation against the golden sets (key now in Keychain)

Worktree: /Users/logankim/AI-Workspaces/omnis.plan-jev (branch plan/jev; the spike scaffold is committed there). Per-branch DB: omnis_test_jev. Keychain item `omnis.vercel.ai_gateway` now exists — read it with `security find-generic-password -s omnis.vercel.ai_gateway -w` (by service name only; ignore the account field). NEVER print, log, or commit the key; never put it in a fixture or a screenshot.

Do: (1) run the spike runner for real (`pnpm spike:jev` or whatever tools/spikes/gate-jev exposes) on eval/auto_archive.jsonl, eval/route_note.jsonl, eval/task.jsonl, eval/followup.jsonl — respect rate limits (429 → backoff), cap spend at $2 (count tokens; Jev is $0.042/1M input); (2) record in tools/spikes/gate-jev/RESULT.md: per set precision/recall/unsafe-count vs the current LLM classifier baseline (run the baseline through the existing harness if numbers are not already recorded), p50/p95 latency, tokens and cost, failure modes with 3 concrete examples each, and a verdict PASS/FAIL against the Phase B gates (auto-archive: precision ≥ 0.97, recall ≥ 0.70, unsafe = 0); (3) fix real API-shape mismatches in packages/agents/src/providers/jev.ts discovered by the live run (update the fixtures from real, redacted responses); (4) if PASS on auto-archive, leave the flag default as is but document in docs/decisions/2026-09-21-jev-decision-tier.md the recommended rollout (flag on for auto-archive first, shadow mode for the others). Commit per slice with the trailers. Acceptance: RESULT.md has real numbers and a verdict; no secret anywhere in the diff (reviewer greps for the key prefix and any 40+ char token); tests green.
