# D6: Monotone base + aurora accent surfaces

Worktree: /Users/logankim/AI-Workspaces/omnis.plan-design-w1 (branch plan/design-w1). Per-branch DB: omnis_test_design_w1. Desktop dev server: `pnpm --filter @omnis/desktop dev` (Vite 5173). For realistic screens use the e2e seed: read tools/e2e/run.ts / tools/e2e/seed.ts / tools/e2e/shots.ts and reuse them (they boot hub + zero-cache + seed omnis_e2e). Playwright is installed (tools/e2e). Project skills live in .claude/skills (hallmark, motion-dev-animations, design-tokens, tailwind-v4-shadcn) and load via the Skill tool. Screenshots go under docs/design/screens/ and are committed.

Hard rules: glass only on rail/toolbar/sheet/palette/floating panels; lists/body opaque; light theme; no new heavy dependencies without checking package.json; never hand-draw brand SVGs; reduced-motion respected; nothing overflows horizontally at 390/768/1024/1440.

## Brief
Logan's directive: the app's background and overall palette go MONOTONE; on specific chrome surfaces (rail plate, popups/sheets, AI panel, command palette, onboarding/empty states, agents tile) the accent follows his reference: a grainy, heavily blurred aurora (deep indigo/cobalt mass, violet haze, warm amber-to-coral bloom, dissolving into paper white) — same sensibility, not identical copies. The structured, implementable direction is in /Users/logankim/AI-Workspaces/Claude/omnis/design/ACCENT-DIRECTION.md (Read it first; also Read ACCENT-BRIEF-fable.md next to it; if ref-aurora-accent.png exists there, Read it). Copy both markdown files into docs/design/ (as ACCENT-DIRECTION.md and ACCENT-BRIEF.md) so they travel with the repo. Implement exactly what ACCENT-DIRECTION.md specifies: (1) the neutral token scale replacing hue-tinted base tokens, (2) the AuroraSurface component/class with the three variants, grain overlay, blur technique and reduced-motion/transparency fallbacks, (3) the surface map (aurora only where allowed; lists/bodies/tables/cards/buttons stay monotone), (4) contrast scrims for text over aurora. Then re-shoot docs/design/screens/inbox-kinso.png, ai-panel.png, palette (⌘K) and onboarding at 1440 and 390 into docs/design/screens/accent/. Read them next to the brief's description and iterate (≤3 rounds) until the mood reads as the reference: quiet, cinematic, analog grain, no hard edges. Tests: RTL for variant class mapping and reduced-motion; Playwright asserts the aurora element is absent inside list rows and message bodies. Commit "US-D06: monotone base + aurora accent surfaces".

## Acceptance (the reviewer will check all of these)
- Every item of the brief implemented; tests named in the brief exist and pass; `pnpm lint && pnpm typecheck` clean; the named screenshots committed and, when Read next to the reference images, actually read as the reference.
- Commit messages as the brief says, with the trailers.
