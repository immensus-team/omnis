# Spike + integration scaffold: Jev (TypeSafe AI "System One" decision model) as omnis's decision tier

Worktree: /Users/logankim/AI-Workspaces/omnis.plan-jev (branch plan/jev). Per-branch DB: omnis_test_jev. Scope: packages/agents (classification/decision call sites), a new provider module, tools/spikes/gate-jev/, eval/ harness usage, docs/decisions/. Do NOT touch apps/desktop or packages/ui.

Context: Jev launched 2026-09-15 — a probabilistic decision model that evaluates declared questions in parallel and returns typed answers + probabilities (no text generation), available via Vercel AI Gateway (https://vercel.com/ai-gateway/models/jev, https://vercel.com/changelog/typesafe-ai-jev-now-available-on-ai-gateway) with community integrations (https://github.com/AnotiaWang/awesome-jev, https://www.scriptbyai.com/jev-resource-list/, Jev Review MCP https://github.com/victorlucss/jev-review-vercel-ai). omnis today makes many yes/no + label decisions with LLM prompts in its loops: work/personal + label classification, auto-archive confidence (safety invariant: VIP/sensitive never auto-archived), draft-worthiness, follow-up nudge, delegation eligibility, sensitivity routing (see docs/spec/A4-agent-layer.md, packages/agents/src/*, eval/*.jsonl golden sets with tools/eval/validate.ts and the eval harnesses landed in W3). No API key exists yet — Logan will add a Vercel AI Gateway key to Keychain as `omnis.vercel.ai_gateway` later; everything must work without it (mocked provider) and light up when it exists.

Deliver, with TDD and commits per slice:
1. Research memo docs/decisions/2026-09-21-jev-decision-tier.md (English): what Jev is, the exact API shape via Vercel AI Gateway (read the docs with WebFetch; cite URLs), typed-question schema, pricing/latency, limits, how community plugins wire it into OpenCode/Codex/Claude Code, and a mapping table "omnis decision → Jev question(s)" for the six decision types above, with the safety rule that Jev never approves an outbound action by itself.
2. packages/agents/src/providers/jev.ts: a `JevDecider` implementing the existing decision interface used by the loops (find the real interface first — e.g. the classify/decide functions the loops call — and adapt; if there is no shared interface, introduce a minimal `Decider` type and route the six call sites through it behind a settings flag `agents.decision_provider = "llm" | "jev"`, default "llm"). Reads the key from Keychain via the existing keychain helper (`omnis.vercel.ai_gateway`); if absent → provider reports unavailable and the router falls back to the current LLM path. Unit tests with a mocked HTTP layer (record the request/response shape from the docs as fixtures).
3. tools/spikes/gate-jev/: an eval runner that scores Jev vs the current classifier on eval/auto_archive.jsonl, eval/route_note.jsonl, eval/task.jsonl, eval/followup.jsonl using the existing validate/harness code (precision/recall/unsafe count, latency, $/1k); runs end-to-end in mock mode now and for real when the key exists (`pnpm spike:jev`). Write tools/spikes/gate-jev/RESULT.md with the mock run and a "pending real key" section.
4. Optional (only if cheap): note in the memo how Jev Review MCP could feed the ds/ reviewer loop; do not install anything.

Acceptance: memo cites real docs; provider + router + flag with tests green (`pnpm lint && pnpm typecheck && pnpm --filter @omnis/agents test` on the per-branch DB); default behavior unchanged (flag off → zero calls to Jev, proven by a test); spike runner works in mock mode; English only; trailers.
