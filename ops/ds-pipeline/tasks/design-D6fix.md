# D6-fix: one remaining defect from the D6 review (narrow-tier aurora leak)

Worktree: /Users/logankim/AI-Workspaces/omnis.plan-design-w1 (branch plan/design-w1), per-branch DB omnis_test_design_w1. D6 (monotone base + aurora surfaces) is otherwise approved; fix ONLY this, with a regression test, then re-shoot docs/design/screens/accent/inbox-kinso-390.png and rail-zoom.png and commit ('US-D06: narrow tier — no aurora boxes outside the plate').

## Defect (verbatim from the Opus review)
- apps/desktop/src/app.css:2108 — In the narrow tier (@container shell max-width: 899.98px) the block dissolves the rail wrapper with `.aurora.channel-rail__aurora { display: contents }` and then turns off only `.aurora__mass` and `.aurora__grain`. `.aurora::before` (the mist field) and `.aurora::after` (the vignette) still generate boxes and fall into exactly the trap the comment at 2097-2103 describes: with the wrapper out of the box tree they resolve against `.channel-rail`, which is `position: fixed` and full-width. Measured with Playwright against `pnpm --filter @omnis/desktop dev` at 390 and 768: wrapper display=contents, ::before display=block opacity=0.34 filter=blur(6px), containing block = .channel-rail at 390x57 and 768x57. A butter-to-peach radial therefore washes the whole width of the bottom bar with the vignette fading over it — visible in all four committed narrow shots (docs/design/screens/accent/inbox-kinso-390.png, ai-panel-390.png, palette-390.png, agents-filter-390.png) as a warm bloom under the tiles. This breaks brief item 3 (the surface map: the aurora belongs to the rail plate's own footprint, ACCENT-DIRECTION.md §3.2/§4.1.1 'no bleed'), and the code's own comment claims 'the bar's aurora is off' when it is not. Fix: extend the same block to the pseudo-elements — `.aurora.channel-rail__aurora::before, .aurora.channel-rail__aurora::after { display: none; }` — and update the comment to name all four layers, then re-shoot the four 390 screens (and re-check 768).
## Acceptance
- At 390 and 768 (container shell narrow tier) no `.aurora::before` / `.aurora::after` box is generated for the rail wrapper (either the wrapper is not `display: contents`, or all four aurora layers are disabled there); measured with a Playwright check added to tools/e2e/aurora.spec.ts (getComputedStyle of ::before/::after display === 'none' or the wrapper keeps its own box) that passes; screenshots show no stray blurred band across the shell at 390/768; lint/typecheck/tests green; trailers present; tree clean.
