# US-B44 cost report job — finalize after two review rejections

Worktree: /Users/logankim/AI-Workspaces/omnis.plan-w4a-cost (branch plan/w4a-cost). Per-branch DB: omnis_test_w4a_cost. Plan: docs/superpowers/plans/2026-09-20-phase-b-ops.md "### Task 6" (+ docs/superpowers/plans/2026-09-20-phase-b-interfaces-delta.md, backlog row US-B44).

The previous reviews rejected: (1) packages/kernel/src/jobs/cost-report.ts computed the aggregation window with UTC calendar methods while the scheduler evaluates cron on the Asia/Seoul calendar (see packages/kernel/src/cron.ts SEOUL_OFFSET_MS) — the monthly window must be the Seoul calendar month; (2) packages/kernel/test/integration/cost-report-job.test.ts deleted the migration-seeded `cost_report_monthly` jobs row (seeded by packages/db/migrations/0012_jobs_phase_b.sql) — tests must never delete migration-seeded rows (HEAD claims this is fixed: verify). Do: read the plan task, the current diff vs main, and both points; fix anything still wrong; make sure the job's window, the report content, and the settings write (`cost.last_state`) match the plan; run `pnpm --filter @omnis/kernel test` (and the full `pnpm test` if kernel touches shared code) on the per-branch DB; lint + typecheck; commit. English only.
