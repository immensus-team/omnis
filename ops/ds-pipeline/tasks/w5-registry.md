# W5 — US-B45 hub adapter registry + bootstrap wiring (channels plan Task 17)

Worktree: /Users/logankim/AI-Workspaces/omnis.plan-w5-registry (branch plan/w5-registry). Per-branch DB: omnis_test_w5_registry. Plan: docs/superpowers/plans/2026-09-20-phase-b-channels.md "### Task 17" (read it fully, plus docs/superpowers/plans/2026-09-20-phase-b-interfaces-delta.md, the backlog row US-B45 in 2026-09-20-phase-b-backlog.md, and the review notes in 2026-09-20-phase-b-plans-review.md). All prerequisites (channels Tasks 3, 8, 14; the adapter factories; recordAdapterHealth) are on main — grep for the real exported names first and adapt the plan's names to what exists (record deviations).

Deliver: the hub adapter registry (factory table for slack/gmail/google-calendar/outlook/telegram(/hermes runtime if the plan says so)), bootstrap wiring in apps/hub/src/main.ts behind the existing settings/env (no real credentials exist yet — registry must boot cleanly with zero connected accounts and log which adapters are configured), tests per the plan (unit + hub integration on the per-branch DB). ALSO wire `registerCostReportJob` (packages/kernel/src/jobs/cost-report.ts, merged in W4a) into main.ts next to registerHealthcheckJob / registerCostDailyJob, with a test that the job is registered. Translate any Korean comments in apps/hub/src/main.ts while you are there. Commit per the plan's commit steps with the trailers. Acceptance: plan Task 17 checklist met; `pnpm lint && pnpm typecheck && pnpm --filter @omnis/hub test` green; hub starts (`pnpm --filter @omnis/hub dev` or its start script) with an empty accounts table and /health returns ok.
