<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<!-- US-B41. LaunchAgent (gui session, no sudo) per the measure-first principle — reading the Keychain requires a GUI
     session (RUNBOOK §4). The folder name follows the backlog deliverable path (ops/mini/LaunchDaemons/*.plist) as-is.
     `ops/mini/install.sh backup` substitutes __OMNIS_ROOT__ and __HOME__ and installs it into ~/Library/LaunchAgents
     (install.sh looks at this folder next after ops/mini/). It is a calendar job, so no kickstart is performed. -->
<plist version="1.0">
<dict>
  <key>Label</key><string>com.omnis.backup</string>
  <key>ProgramArguments</key>
  <array>
    <string>/bin/bash</string>
    <string>__OMNIS_ROOT__/ops/scripts/omnis-backup.sh</string>
  </array>
  <key>StartCalendarInterval</key>
  <dict><key>Hour</key><integer>3</integer><key>Minute</key><integer>0</integer></dict>
  <key>WorkingDirectory</key><string>__OMNIS_ROOT__</string>
  <key>StandardOutPath</key><string>__HOME__/Library/Logs/omnis/backup.log</string>
  <key>StandardErrorPath</key><string>__HOME__/Library/Logs/omnis/backup.err.log</string>
</dict>
</plist>
