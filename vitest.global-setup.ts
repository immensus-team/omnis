/**
 * 자리표시자 — Task 2(`2026-09-20-phase-a-kernel-and-db.md`)가 `omnis_test` 리셋 + `migrate()`로 채운다.
 * Task 1에는 integration 테스트가 없어 이 훅이 할 일도 없다. 두 설정(packages/db/vitest.config.ts,
 * vitest.workspace.ts)이 이 경로를 참조하므로 로딩만 통과시키면 된다.
 */
export default function setup(): void {}
